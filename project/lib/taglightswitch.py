
class TagLightSwitch:

    # take AWS tag name (key) and body (value), parse into start,stop times
    @staticmethod
    def tag_to_dict(name, body):
        range_dict = {}
        return range_dict

